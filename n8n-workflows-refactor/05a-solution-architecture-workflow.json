{
  "id": "smxRuLpxe8181lbw",
  "name": "Solution Architecture Pipeline",
  "nodes": [
    {
      "parameters": {
        "inputSource": "passthrough"
      },
      "id": "trigger",
      "name": "Execute Workflow Trigger",
      "type": "n8n-nodes-base.executeWorkflowTrigger",
      "typeVersion": 1.1,
      "position": [0, 304]
    },
    {
      "parameters": {
        "assignments": {
          "assignments": [
            { "name": "projectSlug", "value": "={{ $json.projectSlug }}", "type": "string" },
            { "name": "inputJobId", "value": "={{ $json.inputJobId || null }}", "type": "string" },
            { "name": "comments", "value": "={{ $json.comments || null }}", "type": "string" },
            { "name": "ollamaModel", "value": "llama3.1:8b-instruct-q4_K_M", "type": "string" },
            { "name": "ollamaTemperature", "value": 0, "type": "number" },
            { "name": "ollamaSeed", "value": 42, "type": "number" }
          ]
        },
        "options": {}
      },
      "id": "init-params",
      "name": "Initialize Parameters",
      "type": "n8n-nodes-base.set",
      "typeVersion": 3.4,
      "position": [240, 304]
    },
    {
      "parameters": {
        "operation": "executeQuery",
        "query": "SELECT p.id, p.slug, p.name FROM architecture.projects p WHERE p.slug = $1;",
        "options": { "queryReplacement": "={{ [ $json.projectSlug ] }}" }
      },
      "id": "get-project",
      "name": "Load Project",
      "type": "n8n-nodes-base.postgres",
      "typeVersion": 2.6,
      "position": [480, 304],
      "credentials": { "postgres": { "id": "M5eIfNwtZu8y3dOp", "name": "Postgres account" } }
    },
    {
      "parameters": {
        "operation": "executeQuery",
        "query": "SELECT id FROM architecture.jobs WHERE project_id = $1 AND stage = 'architecture' AND status = 'completed' ORDER BY created_at DESC LIMIT 1;",
        "options": { "queryReplacement": "={{ [ $json.id ] }}" }
      },
      "id": "get-arch-job",
      "name": "Get Latest Arch Job",
      "type": "n8n-nodes-base.postgres",
      "typeVersion": 2.6,
      "position": [720, 304],
      "credentials": { "postgres": { "id": "M5eIfNwtZu8y3dOp", "name": "Postgres account" } },
      "alwaysOutputData": true
    },
    {
      "parameters": {
        "operation": "executeQuery",
        "query": "INSERT INTO architecture.jobs (project_id, stage, input_job_id, status, meta) VALUES ($1, 'solution-architecture', $2, 'running', $3::jsonb) RETURNING id, stage, status;",
        "options": { "queryReplacement": "={{ [ $('Load Project').first().json.id, $json.id || null, JSON.stringify({ comments: $('Initialize Parameters').first().json.comments }) ] }}" }
      },
      "id": "create-job",
      "name": "Create Solution Arch Job",
      "type": "n8n-nodes-base.postgres",
      "typeVersion": 2.6,
      "position": [960, 304],
      "credentials": { "postgres": { "id": "M5eIfNwtZu8y3dOp", "name": "Postgres account" } }
    },
    {
      "parameters": {
        "jsCode": "const init = $('Initialize Parameters').first().json;\nconst project = $('Load Project').first().json;\nconst job = $json;\nreturn [{ json: { ...init, projectId: project.id, projectSlug: project.slug, projectName: project.name || project.slug, jobId: job.id } }];"
      },
      "id": "pass-context",
      "name": "Pass Context",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [1200, 304]
    },
    {
      "parameters": {
        "operation": "executeQuery",
        "query": "SELECT DISTINCT ON (a.type) a.type, a.content FROM architecture.artifacts a WHERE a.project_id = $1 AND a.type IN ('brd', 'business_arch', 'application_arch', 'data_arch', 'infrastructure_arch') ORDER BY a.type, a.version DESC;",
        "options": { "queryReplacement": "={{ [ $json.projectId ] }}" }
      },
      "id": "load-artifacts",
      "name": "Load Architecture Artifacts",
      "type": "n8n-nodes-base.postgres",
      "typeVersion": 2.6,
      "position": [1440, 304],
      "credentials": { "postgres": { "id": "M5eIfNwtZu8y3dOp", "name": "Postgres account" } },
      "alwaysOutputData": true
    },
    {
      "parameters": {
        "jsCode": "const ctx = $('Pass Context').first().json;\nconst rows = $input.all().map(i => i.json);\nconst byType = {};\nfor (const r of rows) byType[r.type] = r.content;\nreturn [{ json: { ...ctx, artifacts: byType } }];"
      },
      "id": "collect-artifacts",
      "name": "Collect Artifacts",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [1680, 304]
    },
    {
      "parameters": {
        "method": "POST",
        "url": "https://ollama.socrates-hlapolosa.org/api/chat",
        "sendBody": true,
        "specifyBody": "json",
        "jsonBody": "={{ {\n  model: $('Initialize Parameters').first().json.ollamaModel,\n  messages: [\n    { role: 'system', content: 'You are a Solution Architect producing integration artifacts. Output JSON with: integrationStyle (rest_only|events_only|hybrid), solutionMarkdown (starts with \"# Solution Architecture:\"), openapiYaml (OpenAPI 3.0 spec if REST), asyncapiYaml (AsyncAPI 2.0 spec if events), avroSchemasJson (Avro schemas object if events), cloudEventsMarkdown (Cloud Events documentation if events), sqlSchemaSql (PostgreSQL DDL). Include all relevant artifacts based on integrationStyle. No code fences anywhere.' },\n    { role: 'user', content: 'PROJECT: ' + $json.projectName + '\\nBRD: ' + JSON.stringify($json.artifacts.brd || {}).slice(0,6000) + '\\nAPP_ARCH: ' + JSON.stringify($json.artifacts.application_arch || {}).slice(0,6000) + '\\nDATA_ARCH: ' + JSON.stringify($json.artifacts.data_arch || {}).slice(0,4000) + '\\nINFRA_ARCH: ' + JSON.stringify($json.artifacts.infrastructure_arch || {}).slice(0,4000) }\n  ],\n  stream: false,\n  format: { type: 'object', required: ['integrationStyle','solutionMarkdown','sqlSchemaSql'], properties: { integrationStyle: { type: 'string', enum: ['rest_only','events_only','hybrid'] }, solutionMarkdown: { type: 'string' }, openapiYaml: { type: ['string','null'] }, asyncapiYaml: { type: ['string','null'] }, avroSchemasJson: { type: ['object','null'] }, cloudEventsMarkdown: { type: ['string','null'] }, sqlSchemaSql: { type: 'string' } } },\n  options: { temperature: $('Initialize Parameters').first().json.ollamaTemperature, seed: $('Initialize Parameters').first().json.ollamaSeed }\n} }}",
        "options": { "timeout": 300000 }
      },
      "id": "gen-solution",
      "name": "Generate Solution Package",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.3,
      "position": [1920, 304],
      "retryOnFail": true
    },
    {
      "parameters": {
        "jsCode": "const raw = $json.message?.content;\nlet p; try { p = typeof raw === 'string' ? JSON.parse(raw) : raw; } catch (e) { return [{ json: { valid: false, error: 'JSON parse failed: ' + e.message } }]; }\nconst fnv1a = (s) => { let h = 0x811c9dc5; for (let i = 0; i < (s||'').length; i++) { h ^= s.charCodeAt(i); h = (h * 0x01000193) >>> 0; } return ('0000000' + h.toString(16)).slice(-8); };\nconst valid = p.solutionMarkdown && p.solutionMarkdown.startsWith('# Solution Architecture:');\nreturn [{ json: {\n  valid,\n  integrationStyle: p.integrationStyle || 'rest_only',\n  solutionMarkdown: p.solutionMarkdown || '',\n  openapiYaml: p.openapiYaml || null,\n  asyncapiYaml: p.asyncapiYaml || null,\n  avroSchemasJson: p.avroSchemasJson || null,\n  cloudEventsMarkdown: p.cloudEventsMarkdown || null,\n  sqlSchemaSql: p.sqlSchemaSql || '',\n  hashes: {\n    solution: fnv1a(p.solutionMarkdown),\n    openapi: fnv1a(p.openapiYaml),\n    asyncapi: fnv1a(p.asyncapiYaml),\n    avro: fnv1a(JSON.stringify(p.avroSchemasJson)),\n    cloudEvents: fnv1a(p.cloudEventsMarkdown),\n    sql: fnv1a(p.sqlSchemaSql)\n  }\n} }];"
      },
      "id": "val-solution",
      "name": "Validate Solution",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [2160, 304]
    },
    {
      "parameters": {
        "jsCode": "const j = $json;\nif (!j.valid) return [];\nconst ctx = $('Pass Context').first().json;\nconst meta = { jobId: ctx.jobId, stage: 'solution-architecture', integrationStyle: j.integrationStyle };\nconst out = [];\n\n// Always include solution architecture markdown\nout.push({ json: { artifactType: 'solution_arch_md', content: { markdown: j.solutionMarkdown }, contentHash: j.hashes.solution, meta } });\n\n// REST artifacts\nif (j.openapiYaml) out.push({ json: { artifactType: 'openapi_yaml', content: { yaml: j.openapiYaml }, contentHash: j.hashes.openapi, meta } });\n\n// Event-driven artifacts\nif (j.asyncapiYaml) out.push({ json: { artifactType: 'asyncapi_yaml', content: { yaml: j.asyncapiYaml }, contentHash: j.hashes.asyncapi, meta } });\nif (j.avroSchemasJson) out.push({ json: { artifactType: 'avro_schemas_json', content: { schemas: j.avroSchemasJson }, contentHash: j.hashes.avro, meta } });\nif (j.cloudEventsMarkdown) out.push({ json: { artifactType: 'cloud_events_md', content: { markdown: j.cloudEventsMarkdown }, contentHash: j.hashes.cloudEvents, meta } });\n\n// Database artifacts\nif (j.sqlSchemaSql) out.push({ json: { artifactType: 'sql_schema_sql', content: { sql: j.sqlSchemaSql }, contentHash: j.hashes.sql, meta } });\n\nreturn out;"
      },
      "id": "explode-solution",
      "name": "Explode Solution Artifacts",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [2400, 304]
    },
    {
      "parameters": {
        "operation": "executeQuery",
        "query": "WITH next_version AS (SELECT COALESCE(MAX(version), 0) + 1 AS v FROM architecture.artifacts WHERE project_id = $1 AND type = $2) INSERT INTO architecture.artifacts (project_id, type, version, content, content_hash, meta) SELECT $1, $2, next_version.v, $3::jsonb, $4, $5::jsonb FROM next_version RETURNING id, version;",
        "options": { "queryReplacement": "={{ [ $('Pass Context').first().json.projectId, $json.artifactType, JSON.stringify($json.content), $json.contentHash, JSON.stringify($json.meta) ] }}" }
      },
      "id": "store-solution",
      "name": "Store Solution Artifacts",
      "type": "n8n-nodes-base.postgres",
      "typeVersion": 2.6,
      "position": [2640, 304],
      "credentials": { "postgres": { "id": "M5eIfNwtZu8y3dOp", "name": "Postgres account" } }
    },
    {
      "parameters": {
        "jsCode": "const ctx = $('Pass Context').first().json;\nconst sol = $('Validate Solution').first().json;\nconst projectSlug = ctx.projectSlug;\nconst jobId = ctx.jobId;\nconst repoDir = '/opt/workspace/software-delivery';\nconst projectDir = repoDir + '/' + projectSlug + '-docs';\nconst remote = 'git@github.com:shlapolosa/software-delivery.git';\n\n// Helper to safely escape heredoc content\nconst safeHeredoc = (content, marker) => {\n  if (!content) return '';\n  return String(content).replace(new RegExp(marker, 'g'), marker + '_ESCAPED');\n};\n\nlet sh = `set -e\nmkdir -p /opt/workspace\n\nif [ ! -d '${repoDir}/.git' ]; then\n  git clone '${remote}' '${repoDir}'\nfi\ncd '${repoDir}'\ngit fetch origin --prune\n\ngit checkout -B '${projectSlug}' 'origin/${projectSlug}' 2>/dev/null || git checkout -B '${projectSlug}'\nmkdir -p '${projectDir}/docs' '${projectDir}/api' '${projectDir}/db'\n`;\n\n// Solution Architecture markdown\nsh += `cat > '${projectDir}/docs/solution-architecture.md' <<'EOFSOL_${jobId}'\n${safeHeredoc(sol.solutionMarkdown, 'EOFSOL_' + jobId)}\nEOFSOL_${jobId}\n`;\n\n// REST API spec\nif (sol.openapiYaml) {\n  sh += `cat > '${projectDir}/api/openapi.yaml' <<'EOFAPI_${jobId}'\n${safeHeredoc(sol.openapiYaml, 'EOFAPI_' + jobId)}\nEOFAPI_${jobId}\n`;\n}\n\n// Event-driven specs\nif (sol.asyncapiYaml) {\n  sh += `cat > '${projectDir}/api/asyncapi.yaml' <<'EOFASYNC_${jobId}'\n${safeHeredoc(sol.asyncapiYaml, 'EOFASYNC_' + jobId)}\nEOFASYNC_${jobId}\n`;\n}\n\nif (sol.avroSchemasJson) {\n  sh += `cat > '${projectDir}/api/avro-schemas.json' <<'EOFAVRO_${jobId}'\n${safeHeredoc(JSON.stringify(sol.avroSchemasJson, null, 2), 'EOFAVRO_' + jobId)}\nEOFAVRO_${jobId}\n`;\n}\n\nif (sol.cloudEventsMarkdown) {\n  sh += `cat > '${projectDir}/docs/cloud-events.md' <<'EOFCE_${jobId}'\n${safeHeredoc(sol.cloudEventsMarkdown, 'EOFCE_' + jobId)}\nEOFCE_${jobId}\n`;\n}\n\n// Database schema\nif (sol.sqlSchemaSql) {\n  sh += `cat > '${projectDir}/db/schema.sql' <<'EOFSQL_${jobId}'\n${safeHeredoc(sol.sqlSchemaSql, 'EOFSQL_' + jobId)}\nEOFSQL_${jobId}\n`;\n}\n\nsh += `\ngit add '${projectSlug}-docs'\ngit commit -m 'Add solution architecture for ${projectSlug} (job ${jobId})' || true\ngit push -u origin '${projectSlug}'\n`;\n\nreturn [{ json: { ...ctx, integrationStyle: sol.integrationStyle, gitScript: sh } }];"
      },
      "id": "build-git-script",
      "name": "Build Git Script",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [2880, 304]
    },
    {
      "parameters": {
        "authentication": "privateKey",
        "command": "={{ $json.gitScript }}"
      },
      "id": "ssh-commit",
      "name": "SSH: Commit Solution Files",
      "type": "n8n-nodes-base.ssh",
      "typeVersion": 1,
      "position": [3120, 304],
      "credentials": { "sshPrivateKey": { "id": "SxKNiry9nQQ96pnU", "name": "SSH Private Key account" } }
    },
    {
      "parameters": {
        "operation": "executeQuery",
        "query": "UPDATE architecture.jobs SET status = 'completed', completed_at = now() WHERE id = $1 RETURNING id, status;",
        "options": { "queryReplacement": "={{ [ $('Pass Context').first().json.jobId ] }}" }
      },
      "id": "update-job",
      "name": "Update Job Status",
      "type": "n8n-nodes-base.postgres",
      "typeVersion": 2.6,
      "position": [3360, 304],
      "credentials": { "postgres": { "id": "M5eIfNwtZu8y3dOp", "name": "Postgres account" } }
    },
    {
      "parameters": {
        "jsCode": "const ctx = $('Pass Context').first().json;\nconst sol = $('Validate Solution').first().json;\nreturn [{ json: {\n  success: true,\n  jobId: ctx.jobId,\n  projectSlug: ctx.projectSlug,\n  stage: 'solution-architecture',\n  integrationStyle: sol.integrationStyle,\n  artifacts: {\n    solutionArch: ctx.projectSlug + '-docs/docs/solution-architecture.md',\n    openapi: sol.openapiYaml ? ctx.projectSlug + '-docs/api/openapi.yaml' : null,\n    asyncapi: sol.asyncapiYaml ? ctx.projectSlug + '-docs/api/asyncapi.yaml' : null,\n    avroSchemas: sol.avroSchemasJson ? ctx.projectSlug + '-docs/api/avro-schemas.json' : null,\n    cloudEvents: sol.cloudEventsMarkdown ? ctx.projectSlug + '-docs/docs/cloud-events.md' : null,\n    sqlSchema: sol.sqlSchemaSql ? ctx.projectSlug + '-docs/db/schema.sql' : null\n  }\n} }];"
      },
      "id": "return-result",
      "name": "Return Result",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [3600, 304]
    }
  ],
  "connections": {
    "Execute Workflow Trigger": { "main": [[{ "node": "Initialize Parameters", "type": "main", "index": 0 }]] },
    "Initialize Parameters": { "main": [[{ "node": "Load Project", "type": "main", "index": 0 }]] },
    "Load Project": { "main": [[{ "node": "Get Latest Arch Job", "type": "main", "index": 0 }]] },
    "Get Latest Arch Job": { "main": [[{ "node": "Create Solution Arch Job", "type": "main", "index": 0 }]] },
    "Create Solution Arch Job": { "main": [[{ "node": "Pass Context", "type": "main", "index": 0 }]] },
    "Pass Context": { "main": [[{ "node": "Load Architecture Artifacts", "type": "main", "index": 0 }]] },
    "Load Architecture Artifacts": { "main": [[{ "node": "Collect Artifacts", "type": "main", "index": 0 }]] },
    "Collect Artifacts": { "main": [[{ "node": "Generate Solution Package", "type": "main", "index": 0 }]] },
    "Generate Solution Package": { "main": [[{ "node": "Validate Solution", "type": "main", "index": 0 }]] },
    "Validate Solution": { "main": [[{ "node": "Explode Solution Artifacts", "type": "main", "index": 0 }]] },
    "Explode Solution Artifacts": { "main": [[{ "node": "Store Solution Artifacts", "type": "main", "index": 0 }]] },
    "Store Solution Artifacts": { "main": [[{ "node": "Build Git Script", "type": "main", "index": 0 }]] },
    "Build Git Script": { "main": [[{ "node": "SSH: Commit Solution Files", "type": "main", "index": 0 }]] },
    "SSH: Commit Solution Files": { "main": [[{ "node": "Update Job Status", "type": "main", "index": 0 }]] },
    "Update Job Status": { "main": [[{ "node": "Return Result", "type": "main", "index": 0 }]] }
  },
  "settings": {
    "executionOrder": "v1",
    "timezone": "UTC",
    "callerPolicy": "workflowsFromSameOwner"
  }
}
